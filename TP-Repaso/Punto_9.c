/*  En una ciudad hay un control de camaras de seguridad que lee las patentes de autos que ingresan.
    al leer la patente, la misma se transforma en un numero entero.
    es necesario mostrar por pantalla las patentes correctas a partir del numero que envia el lector.

    una patente esta formada por 2 letras mayusculas seguidas de 2 numeros.
    por ejemplo: AF56

    El numero que envia el lector tiene 8 digitos de los cuales los 2 primeros pares de numeros
    corresponden a las 2 letras y los 2 utimos pares de numeros son el numero de la patente.
    los pares de numeros correspondena a caracteres ASCII.

    realize un algoritmo que lea los numeros,muestre la patente y si hay caracteres que no son alfanumericos
    debe indicar un error de lectura.
    el programa finaliza con el numero de patente 0.

    ejemplo
    El lector nos envia un numero entero: 67785156
    la patente correcta a mostrar:        CN38

ALGORITMO: Lectoe_Patentes
ENTRADA: numeros_Patentes
var_aux: anterior
Salidas:Patente

A1_LEER (numeros_Patentes)
A2_ While (numeros_Patentes <> 0)
        Si numerosPatentes < 8 ENTONCES
            Si numerosPatentes = isdigit(numerosPatentes) ENTONCES
                   Si numerosPatentes 


*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main () { 

char Patente;
int cantCaracteres = 0;





    





}