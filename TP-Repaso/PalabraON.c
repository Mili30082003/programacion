#include <stdio.h>

int main () {
char frase;
char MF = '.';
char anterior;
int cantON = 0;

printf ("Ingresar una frase terminada en un punto: ");
frase = getchar ();
//scanf ("%c", &frase);

while (frase != MF)
{
   while (frase != MF && frase != ' ')
   {
    
   }
   


    }
    
    printf ("La cantidad de palabras terminadas en on es: %d", cantON);



return 0;

}